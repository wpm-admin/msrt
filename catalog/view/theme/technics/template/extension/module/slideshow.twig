<div class="swiper hero-slider" id="slideshow{{ module }}">
    <div class="swiper-wrapper">
        {% for banner in banners %}
            <div class="swiper-slide">
                <img src="{{ banner.image }}" class="hero-bg" alt="{{ banner.title }}">
            </div>
        {% endfor %}
    </div>
</div>