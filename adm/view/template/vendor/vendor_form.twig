{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="pull-right">
        <button type="submit" form="form-vendor" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
        <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
      <h1>{{ heading_title }}</h1>
      <ul class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
        <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ul>
    </div>
  </div>
  <div class="container-fluid"> {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
      <button type="button" class="close" data-dismiss="alert">&times;</button>
    </div>
    {% endif %}
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-pencil"></i> {{ text_form }}</h3>
      </div>
      <div class="panel-body">
        <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-vendor" class="form-horizontal">
          <ul class="nav nav-tabs">
            <li class="active"><a href="#tab-general" data-toggle="tab">{{ tab_general }}</a></li>
            <li><a href="#tab-affiliate" data-toggle="tab">{{ tab_affiliate }}</a></li>
            {% if vendor_id %}
              <li><a href="#tab-products" data-toggle="tab">Products</a></li>
            <li><a href="#tab-history" data-toggle="tab">{{ tab_history }}</a></li>
            <li><a href="#tab-transaction" data-toggle="tab">{{ tab_transaction }}</a></li>
            <li><a href="#tab-reward" data-toggle="tab">{{ tab_reward }}</a></li>
            <li><a href="#tab-ip" data-toggle="tab">{{ tab_ip }}</a></li>
            {% endif %}
          </ul>
          <div class="tab-content">
            <div class="tab-pane active" id="tab-general">
              <div class="row">
                <div class="col-sm-2">
                  <ul class="nav nav-pills nav-stacked" id="vendor_address">
                    <li class="active"><a href="#tab-vendor" data-toggle="tab">{{ tab_general }}</a></li>
                    {% set vendor_address_row = 1 %}
                    {% for vendor_address in vendor_addresses %}
                    <li><a href="#tab-vendor_address{{ vendor_address_row }}" data-toggle="tab"><i class="fa fa-minus-circle" onclick="$('#vendor_address a:first').tab('show'); $('#vendor_address a[href=\'#tab-vendor_address{{ vendor_address_row }}\']').parent().remove(); $('#tab-vendor_address{{ vendor_address_row }}').remove();"></i> {{ tab_vendor_address }} {{ vendor_address_row }}</a></li>
                    {% set vendor_address_row = vendor_address_row + 1 %}
                    {% endfor %}
                    <li id="vendor_address-add"><a onclick="addAddress();"><i class="fa fa-plus-circle"></i> {{ button_vendor_address_add }}</a></li>
                  </ul>
                </div>
                <div class="col-sm-10">
                  <div class="tab-content">
                    <div class="tab-pane active" id="tab-vendor">
                      <fieldset>
                        <legend>{{ text_account }}</legend>
                        <div class="form-group">
                          <label class="col-sm-2 control-label" for="input-vendor-group">{{ entry_vendor_group }}</label>
                          <div class="col-sm-10">
                            <select name="vendor_group_id" id="input-vendor-group" class="form-control">
                              {% for vendor_group in vendor_groups %}
                              {% if vendor_group.vendor_group_id == vendor_group_id %}
                              <option value="{{ vendor_group.vendor_group_id }}" selected="selected">{{ vendor_group.name }}</option>
                              {% else %}
                              <option value="{{ vendor_group.vendor_group_id }}">{{ vendor_group.name }}</option>
                              {% endif %}
                              {% endfor %}
                            </select>
                          </div>
                        </div>

                        <div class="form-group">
                          <label class="col-sm-2 control-label" for="input-prefix">Sku prefix add to Product Code</label>
                          <div class="col-sm-10">
                            <input type="text" name="prefix" value="{{ prefix }}" placeholder="Sku prefix add to Product Code" id="input-prefix" class="form-control" />
                            {% if error_prefix %}
                            <div class="text-danger">{{ error_prefix }}</div>
                            {% endif %}</div>
                        </div>

                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-firstname">{{ entry_firstname }}</label>
                          <div class="col-sm-10">
                            <input type="text" name="firstname" value="{{ firstname }}" placeholder="{{ entry_firstname }}" id="input-firstname" class="form-control" />
                            {% if error_firstname %}
                            <div class="text-danger">{{ error_firstname }}</div>
                            {% endif %}</div>
                        </div>
                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-lastname">{{ entry_lastname }}</label>
                          <div class="col-sm-10">
                            <input type="text" name="lastname" value="{{ lastname }}" placeholder="{{ entry_lastname }}" id="input-lastname" class="form-control" />
                            {% if error_lastname %}
                            <div class="text-danger">{{ error_lastname }}</div>
                            {% endif %}</div>
                        </div>
                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-email">{{ entry_email }}</label>
                          <div class="col-sm-10">
                            <input type="text" name="email" value="{{ email }}" placeholder="{{ entry_email }}" id="input-email" class="form-control" />
                            {% if error_email %}
                            <div class="text-danger">{{ error_email }}</div>
                            {% endif %}</div>
                        </div>
                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-telephone">{{ entry_telephone }}</label>
                          <div class="col-sm-10">
                            <input type="text" name="telephone" value="{{ telephone }}" placeholder="{{ entry_telephone }}" id="input-telephone" class="form-control" />
                            {% if error_telephone %}
                            <div class="text-danger">{{ error_telephone }}</div>
                            {% endif %}</div>
                        </div>
                        <div class="form-group ">
                          <label class="col-sm-2 control-label" for="input-telephone2">{{ entry_telephone }} 2</label>
                          <div class="col-sm-10">
                            <input type="text" name="telephone2" value="{{ telephone2 }}" placeholder="{{ entry_telephone }}" id="input-telephone2" class="form-control" />
                            </div>
                        </div>
                        {% for vendor_field in vendor_fields %}
                        {% if vendor_field.location == 'account' %}
                        {% if vendor_field.type == 'select' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <select name="vendor_field[{{ vendor_field.vendor_field_id }}]" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">
                              <option value="">{{ text_select }}</option>
                              {% for vendor_field_value in vendor_field.vendor_field_value %}
                              {% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == account_vendor_field[vendor_field.vendor_field_id] %}
                              <option value="{{ vendor_field_value.vendor_field_value_id }}" selected="selected">{{ vendor_field_value.name }}</option>
                              {% else %}
                              <option value="{{ vendor_field_value.vendor_field_value_id }}">{{ vendor_field_value.name }}</option>
                              {% endif %}
                              {% endfor %}
                            </select>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'radio' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                              <div class="radio"> {% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == account_vendor_field[vendor_field.vendor_field_id] %}
                                <label>
                                  <input type="radio" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                                  {{ vendor_field_value.name }}</label>
                                {% else %}
                                <label>
                                  <input type="radio" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                                  {{ vendor_field_value.name }}</label>
                                {% endif %}</div>
                              {% endfor %} </div>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'checkbox' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                              <div class="checkbox">{% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id in account_vendor_field[vendor_field.vendor_field_id] %}
                                <label>
                                  <input type="checkbox" name="vendor_field[{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                                  {{ vendor_field_value.name }}</label>
                                {% else %}
                                <label>
                                  <input type="checkbox" name="vendor_field[{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                                  {{ vendor_field_value.name }}</label>
                                {% endif %}</div>
                              {% endfor %}</div>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'text' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'textarea' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <textarea name="vendor_field[{{ vendor_field.vendor_field_id }}]" rows="5" placeholder="{{ vendor_field.name }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}</textarea>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'file' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <button type="button" id="button-custom-field{{ vendor_field.vendor_field_id }}" data-loading-text="{{ text_loading }}" class="btn btn-default"><i class="fa fa-upload"></i> {{ button_upload }}</button>
                            <input type="hidden" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id]['code'] ? account_vendor_field[vendor_field.vendor_field_id]['code'] }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" />
                            <span>{{ account_vendor_field[vendor_field.vendor_field_id]['name'] }}</span>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'date' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <div class="input-group date">
                              <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                              <span class="input-group-btn">
                              <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                              </span></div>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'time' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <div class="input-group time">
                              <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="HH:mm" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                              <span class="input-group-btn">
                              <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                              </span></div>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% if vendor_field.type == 'datetime' %}
                        <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                          <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                          <div class="col-sm-10">
                            <div class="input-group datetime">
                              <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD HH:mm" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                              <span class="input-group-btn">
                              <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                              </span></div>
                            {% if error_vendor_field[vendor_field.vendor_field_id] %}
                            <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                            {% endif %}</div>
                        </div>
                        {% endif %}
                        {% endif %}
                        {% endfor %}
                      </fieldset>
                      <fieldset>
                        <legend>{{ text_password }}</legend>
                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-password">{{ entry_password }}</label>
                          <div class="col-sm-10">
                            <input type="password" name="password" value="{{ password }}" placeholder="{{ entry_password }}" id="input-password" class="form-control" autocomplete="off" />
                            {% if error_password %}
                            <div class="text-danger">{{ error_password }}</div>
                            {% endif %}</div>
                        </div>
                        <div class="form-group required">
                          <label class="col-sm-2 control-label" for="input-confirm">{{ entry_confirm }}</label>
                          <div class="col-sm-10">
                            <input type="password" name="confirm" value="{{ confirm }}" placeholder="{{ entry_confirm }}" autocomplete="off" id="input-confirm" class="form-control" />
                            {% if error_confirm %}
                            <div class="text-danger">{{ error_confirm }}</div>
                            {% endif %}</div>
                        </div>
                      </fieldset>
                      <fieldset>
                        <legend>{{ text_other }}</legend>
                        <div class="form-group">
                          <label class="col-sm-2 control-label" for="input-newsletter">{{ entry_newsletter }}</label>
                          <div class="col-sm-10">
                            <select name="newsletter" id="input-newsletter" class="form-control">
                              {% if newsletter %}
                              <option value="1" selected="selected">{{ text_enabled }}</option>
                              <option value="0">{{ text_disabled }}</option>
                              {% else %}
                              <option value="1">{{ text_enabled }}</option>
                              <option value="0" selected="selected">{{ text_disabled }}</option>
                              {% endif %}
                            </select>
                          </div>
                        </div>
                        <div class="form-group">
                          <label class="col-sm-2 control-label" for="input-status">{{ entry_status }}</label>
                          <div class="col-sm-10">
                            <select name="status" id="input-status" class="form-control">
                              {% if status %}
                              <option value="1" selected="selected">{{ text_enabled }}</option>
                              <option value="0">{{ text_disabled }}</option>
                              {% else %}
                              <option value="1">{{ text_enabled }}</option>
                              <option value="0" selected="selected">{{ text_disabled }}</option>
                              {% endif %}
                            </select>
                          </div>
                        </div>
                        <div class="form-group">
                          <label class="col-sm-2 control-label" for="input-safe"><span data-toggle="tooltip" title="{{ help_safe }}">{{ entry_safe }}</span></label>
                          <div class="col-sm-10">
                            <select name="safe" id="input-safe" class="form-control">
                              {% if safe %}
                              <option value="1" selected="selected">{{ text_yes }}</option>
                              <option value="0">{{ text_no }}</option>
                              {% else %}
                              <option value="1">{{ text_yes }}</option>
                              <option value="0" selected="selected">{{ text_no }}</option>
                              {% endif %}
                            </select>
                          </div>
                        </div>
                      </fieldset>
                    </div>
                    {% set vendor_address_row = 1 %}
                    {% for vendor_address in vendor_addresses %}
                    <div class="tab-pane" id="tab-vendor_address{{ vendor_address_row }}">
                      <input type="hidden" name="vendor_address[{{ vendor_address_row }}][vendor_address_id]" value="{{ vendor_address.vendor_address_id }}" />
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-firstname{{ vendor_address_row }}">{{ entry_firstname }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][firstname]" value="{{ vendor_address.firstname }}" placeholder="{{ entry_firstname }}" id="input-firstname{{ vendor_address_row }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row].firstname %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].firstname }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-lastname{{ vendor_address_row }}">{{ entry_lastname }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][lastname]" value="{{ vendor_address.lastname }}" placeholder="{{ entry_lastname }}" id="input-lastname{{ vendor_address_row }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row].lastname %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].lastname }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-company{{ vendor_address_row }}">{{ entry_company }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][company]" value="{{ vendor_address.company }}" placeholder="{{ entry_company }}" id="input-company{{ vendor_address_row }}" class="form-control" />
                        </div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-vendor_address-1{{ vendor_address_row }}">{{ entry_vendor_address_1 }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_address_1]" value="{{ vendor_address.vendor_address_1 }}" placeholder="{{ entry_vendor_address_1 }}" id="input-vendor_address-1{{ vendor_address_row }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row].vendor_address_1 %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_address_1 }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-vendor_address-2{{ vendor_address_row }}">{{ entry_vendor_address_2 }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_address_2]" value="{{ vendor_address.vendor_address_2 }}" placeholder="{{ entry_vendor_address_2 }}" id="input-vendor_address-2{{ vendor_address_row }}" class="form-control" />
                        </div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-city{{ vendor_address_row }}">{{ entry_city }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][city]" value="{{ vendor_address.city }}" placeholder="{{ entry_city }}" id="input-city{{ vendor_address_row }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row].city %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].city }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-postcode{{ vendor_address_row }}">{{ entry_postcode }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][postcode]" value="{{ vendor_address.postcode }}" placeholder="{{ entry_postcode }}" id="input-postcode{{ vendor_address_row }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row].postcode %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].postcode }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-country{{ vendor_address_row }}">{{ entry_country }}</label>
                        <div class="col-sm-10">
                          <select name="vendor_address[{{ vendor_address_row }}][country_id]" id="input-country{{ vendor_address_row }}" onchange="country(this, '{{ vendor_address_row }}', '{{ vendor_address.zone_id }}');" class="form-control">
                            <option value="">{{ text_select }}</option>
                            {% for country in countries %}
                            {% if country.country_id == vendor_address.country_id %}
                            <option value="{{ country.country_id }}" selected="selected">{{ country.name }}</option>
                            {% else %}
                            <option value="{{ country.country_id }}">{{ country.name }}</option>
                            {% endif %}
                            {% endfor %}
                          </select>
                          {% if error_vendor_address[vendor_address_row].country %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].country }}</div>
                          {% endif %}</div>
                      </div>
                      <div class="form-group required">
                        <label class="col-sm-2 control-label" for="input-zone{{ vendor_address_row }}">{{ entry_zone }}</label>
                        <div class="col-sm-10">
                          <select name="vendor_address[{{ vendor_address_row }}][zone_id]" id="input-zone{{ vendor_address_row }}" class="form-control">
                          </select>
                          {% if error_vendor_address[vendor_address_row].zone %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].zone }}</div>
                          {% endif %}</div>
                      </div>
                      {% for vendor_field in vendor_fields %}
                      {% if vendor_field.location == 'vendor_address' %}
                      {% if vendor_field.type == 'select' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <select name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">
                            <option value="">{{ text_select }}</option>
                            {% for vendor_field_value in vendor_field.vendor_field_value %}
                            {% if vendor_address.vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == vendor_address.vendor_field[vendor_field.vendor_field_id] %}
                            <option value="{{ vendor_field_value.vendor_field_value_id }}" selected="selected">{{ vendor_field_value.name }}</option>
                            {% else %}
                            <option value="{{ vendor_field_value.vendor_field_value_id }}">{{ vendor_field_value.name }}</option>
                            {% endif %}
                            {% endfor %}
                          </select>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'radio' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                            <div class="radio"> {% if vendor_address.vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == vendor_address.vendor_field[vendor_field.vendor_field_id] %}
                              <label>
                                <input type="radio" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                                {{ vendor_field_value.name }}</label>
                              {% else %}
                              <label>
                                <input type="radio" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                                {{ vendor_field_value.name }}</label>
                              {% endif %}</div>
                            {% endfor %} </div>
                          {% if error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'checkbox' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                            <div class="checkbox"> {% if vendor_address.vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id in vendor_address.vendor_field[vendor_field.vendor_field_id] %}
                              <label>
                                <input type="checkbox" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                                {{ vendor_field_value.name }}</label>
                              {% else %}
                              <label>
                                <input type="checkbox" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                                {{ vendor_field_value.name }}</label>
                              {% endif %}</div>
                            {% endfor %} </div>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'text' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_address.vendor_field[vendor_field.vendor_field_id] ? vendor_address.vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'textarea' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <textarea name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" rows="5" placeholder="{{ vendor_field.name }}" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">{{ vendor_address.vendor_field[vendor_field.vendor_field_id] ? vendor_address.vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}</textarea>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'file' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <button type="button" id="button-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" data-loading-text="{{ text_loading }}" class="btn btn-default"><i class="fa fa-upload"></i> {{ button_upload }}</button>
                          <input type="hidden" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_address.vendor_field[vendor_field.vendor_field_id]['code'] ? vendor_address.vendor_field[vendor_field.vendor_field_id]['code'] }}" />
                          <span>{{ vendor_address.vendor_field[vendor_field.vendor_field_id]['name'] }}</span>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'date' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <div class="input-group date">
                            <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_address.vendor_field[vendor_field.vendor_field_id] ? vendor_address.vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                            <span class="input-group-btn">
                            <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                            </span></div>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'time' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <div class="input-group time">
                            <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_address.vendor_field[vendor_field.vendor_field_id] ? vendor_address.vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="HH:mm" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                            <span class="input-group-btn">
                            <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                            </span></div>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% if vendor_field.type == 'datetime' %}
                      <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">
                        <label class="col-sm-2 control-label" for="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                        <div class="col-sm-10">
                          <div class="input-group datetime">
                            <input type="text" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_address.vendor_field[vendor_field.vendor_field_id] ? vendor_address.vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD HH:mm" id="input-vendor_address{{ vendor_address_row }}-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                            <span class="input-group-btn">
                            <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                            </span> </div>
                          {% if error_vendor_address[vendor_address_row]['vendor_field'][vendor_field.vendor_field_id] %}
                          <div class="text-danger">{{ error_vendor_address[vendor_address_row].vendor_field[vendor_field.vendor_field_id] }}</div>
                          {% endif %}</div>
                      </div>
                      {% endif %}
                      {% endif %}
                      {% endfor %}
                      <div class="form-group">
                        <label class="col-sm-2 control-label">{{ entry_default }}</label>
                        <div class="col-sm-10">
                          <label class="radio">{% if vendor_address.vendor_address_id == vendor_address_id or not vendor_addresses %}
                            <input type="radio" name="vendor_address[{{ vendor_address_row }}][default]" value="{{ vendor_address_row }}" checked="checked" />
                            {% else %}
                            <input type="radio" name="vendor_address[{{ vendor_address_row }}][default]" value="{{ vendor_address_row }}" />
                            {% endif %}</label>
                        </div>
                      </div>
                    </div>
                    {% set vendor_address_row = vendor_address_row + 1 %}
                    {% endfor %}
                  </div>
                </div>
              </div>
            </div>
            <div class="tab-pane" id="tab-products">
              <div class="table-responsive">
                <table id="provider" class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                      <td class="text-left">#</td>
                      <td class="text-left">Model</td>
                      <td class="text-left">SKU</td>
                      <td class="text-left">Name</td>
                      <td class="text-left">Edit</td>
                    </tr>
                  </thead>
                  <tbody>
			  
                    {% for product in products %}
                      <tr>
                        <td class="text-center" style="width: 120px;">
                          <img src="{{ product.thumb }}" />
                        </td>
                        <td class="text-left">
                          {{ product.model }}
                        </td>
                        <td class="text-left">
                          {{ product.sku }}
                        </td>
                        <td class="text-left">
                          {{ product.name }}
                        </td>
                        <td class="text-right"><a href="{{ product.edit }}" data-toggle="tooltip" title="{{ button_edit }}" class="btn btn-primary"><i class="fa fa-pencil"></i></a></td>
                      </tr>
                    {% endfor %}
                  </tbody>

                </table>
              </div>
            </div>
            <div class="tab-pane" id="tab-affiliate">
              <fieldset>
                <legend>{{ text_affiliate }}</legend>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-company">{{ entry_company }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="company" value="{{ company }}" placeholder="{{ entry_company }}" id="input-company" class="form-control" />
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-website">{{ entry_website }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="website" value="{{ website }}" placeholder="{{ entry_website }}" id="input-website" class="form-control" />
                  </div>
                </div>
                <div class="form-group required">
                  <label class="col-sm-2 control-label" for="input-tracking"><span data-toggle="tooltip" title="{{ help_tracking }}">{{ entry_tracking }}</span></label>
                  <div class="col-sm-10">
                    <input type="text" name="tracking" value="{{ tracking }}" placeholder="{{ entry_tracking }}" id="input-tracking" class="form-control" />
                    {% if error_tracking %}
                    <div class="text-danger">{{ error_tracking }}</div>
                    {% endif %}
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-commission"><span data-toggle="tooltip" title="{{ help_commission }}">{{ entry_commission }}</span></label>
                  <div class="col-sm-10">
                    <input type="text" name="commission" value="{{ commission }}" placeholder="{{ entry_commission }}" id="input-commission" class="form-control" />
                  </div>
                </div>
                {% for vendor_field in vendor_fields %}
                {% if vendor_field.location == 'affiliate' %}
                {% if vendor_field.type == 'select' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <select name="vendor_field[{{ vendor_field.vendor_field_id }}]" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">
                      <option value="">{{ text_select }}</option>
                      {% for vendor_field_value in vendor_field.vendor_field_value %}
                      {% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == account_vendor_field[vendor_field.vendor_field_id] %}
                      <option value="{{ vendor_field_value.vendor_field_value_id }}" selected="selected">{{ vendor_field_value.name }}</option>
                      {% else %}
                      <option value="{{ vendor_field_value.vendor_field_value_id }}">{{ vendor_field_value.name }}</option>
                      {% endif %}
                      {% endfor %}
                    </select>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'radio' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                      <div class="radio">{% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id == account_vendor_field[vendor_field.vendor_field_id] %}
                        <label>
                          <input type="radio" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                          {{ vendor_field_value.name }}</label>
                        {% else %}
                        <label>
                          <input type="radio" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                          {{ vendor_field_value.name }}</label>
                        {% endif %}</div>
                      {% endfor %} </div>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'checkbox' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <div> {% for vendor_field_value in vendor_field.vendor_field_value %}
                      <div class="checkbox">{% if account_vendor_field[vendor_field.vendor_field_id] and vendor_field_value.vendor_field_value_id in account_vendor_field[vendor_field.vendor_field_id] %}
                        <label>
                          <input type="checkbox" name="vendor_field[{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" checked="checked" />
                          {{ vendor_field_value.name }}</label>
                        {% else %}
                        <label>
                          <input type="checkbox" name="vendor_field[{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" />
                          {{ vendor_field_value.name }}</label>
                        {% endif %}</div>
                      {% endfor %} </div>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'text' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'textarea' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <textarea name="vendor_field[{{ vendor_field.vendor_field_id }}]" rows="5" placeholder="{{ vendor_field.name }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}</textarea>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'file' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <button type="button" id="button-custom-field{{ vendor_field.vendor_field_id }}" data-loading-text="{{ text_loading }}" class="btn btn-default"><i class="fa fa-upload"></i> {{ button_upload }}</button>
                    <input type="hidden" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] }}" id="input-custom-field{{ vendor_field.vendor_field_id }}" />
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'date' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <div class="input-group date">
                      <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                      <span class="input-group-btn">
                      <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                      </span></div>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'time' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <div class="input-group time">
                      <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="HH:mm" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                      <span class="input-group-btn">
                      <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                      </span></div>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% if vendor_field.type == 'datetime' %}
                <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order }}">
                  <label class="col-sm-2 control-label" for="input-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name }}</label>
                  <div class="col-sm-10">
                    <div class="input-group datetime">
                      <input type="text" name="vendor_field[{{ vendor_field.vendor_field_id }}]" value="{{ account_vendor_field[vendor_field.vendor_field_id] ? account_vendor_field[vendor_field.vendor_field_id] : vendor_field.value }}" placeholder="{{ vendor_field.name }}" data-date-format="YYYY-MM-DD HH:mm" id="input-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />
                      <span class="input-group-btn">
                      <button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button>
                      </span></div>
                    {% if error_vendor_field[vendor_field.vendor_field_id] %}
                    <div class="text-danger">{{ error_vendor_field[vendor_field.vendor_field_id] }}</div>
                    {% endif %}</div>
                </div>
                {% endif %}
                {% endif %}
                {% endfor %}
              </fieldset>
              <fieldset>
                <legend>{{ text_payment }}</legend>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-tax">{{ entry_tax }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="tax" value="{{ tax }}" placeholder="{{ entry_tax }}" id="input-tax" class="form-control" />
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label">{{ entry_payment }}</label>
                  <div class="col-sm-10">
                    <div class="radio">
                      <label>{% if payment == 'cheque' %}
                        <input type="radio" name="payment" value="cheque" checked="checked" />
                        {% else %}
                        <input type="radio" name="payment" value="cheque" />
                        {% endif %}
                        {{ text_cheque }}</label>
                    </div>
                    <div class="radio">
                      <label> {% if payment == 'paypal' %}
                        <input type="radio" name="payment" value="paypal" checked="checked" />
                        {% else %}
                        <input type="radio" name="payment" value="paypal" />
                        {% endif %}
                        {{ text_paypal }}</label>
                    </div>
                    <div class="radio">
                      <label> {% if payment == 'bank' %}
                        <input type="radio" name="payment" value="bank" checked="checked" />
                        {% else %}
                        <input type="radio" name="payment" value="bank" />
                        {% endif %}
                        {{ text_bank }}</label>
                    </div>
                  </div>
                </div>
                <div id="payment-cheque" class="payment">
                  <div class="form-group required">
                    <label class="col-sm-2 control-label" for="input-cheque">{{ entry_cheque }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="cheque" value="{{ cheque }}" placeholder="{{ entry_cheque }}" id="input-cheque" class="form-control" />
                      {% if error_cheque %}
                      <div class="text-danger">{{ error_cheque }}</div>
                      {% endif %}</div>
                  </div>
                </div>
                <div id="payment-paypal" class="payment">
                  <div class="form-group required">
                    <label class="col-sm-2 control-label" for="input-paypal">{{ entry_paypal }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="paypal" value="{{ paypal }}" placeholder="{{ entry_paypal }}" id="input-paypal" class="form-control" />
                      {% if error_paypal %}
                      <div class="text-danger">{{ error_paypal }}</div>
                      {% endif %}</div>
                  </div>
                </div>
                <div id="payment-bank" class="payment">
                  <div class="form-group">
                    <label class="col-sm-2 control-label" for="input-bank-name">{{ entry_bank_name }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="bank_name" value="{{ bank_name }}" placeholder="{{ entry_bank_name }}" id="input-bank-name" class="form-control" />
                    </div>
                  </div>
                  <div class="form-group">
                    <label class="col-sm-2 control-label" for="input-bank-branch-number">{{ entry_bank_branch_number }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="bank_branch_number" value="{{ bank_branch_number }}" placeholder="{{ entry_bank_branch_number }}" id="input-bank-branch-number" class="form-control" />
                    </div>
                  </div>
                  <div class="form-group">
                    <label class="col-sm-2 control-label" for="input-bank-swift-code">{{ entry_bank_swift_code }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="bank_swift_code" value="{{ bank_swift_code }}" placeholder="{{ entry_bank_swift_code }}" id="input-bank-swift-code" class="form-control" />
                    </div>
                  </div>
                  <div class="form-group required">
                    <label class="col-sm-2 control-label" for="input-bank-account-name">{{ entry_bank_account_name }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="bank_account_name" value="{{ bank_account_name }}" placeholder="{{ entry_bank_account_name }}" id="input-bank-account-name" class="form-control" />
                      {% if error_bank_account_name %}
                      <div class="text-danger">{{ error_bank_account_name }}</div>
                      {% endif %}</div>
                  </div>
                  <div class="form-group required">
                    <label class="col-sm-2 control-label" for="input-bank-account-number">{{ entry_bank_account_number }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="bank_account_number" value="{{ bank_account_number }}" placeholder="{{ entry_bank_account_number }}" id="input-bank-account-number" class="form-control" />
                      {% if error_bank_account_number %}
                      <div class="text-danger">{{ error_bank_account_number }}</div>
                      {% endif %}</div>
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-affiliate">{{ entry_status }}</label>
                  <div class="col-sm-10">
                    <select name="affiliate" id="input-affiliate" class="form-control">
                      {% if affiliate %}
                      <option value="1" selected="selected">{{ text_enabled }}</option>
                      <option value="0">{{ text_disabled }}</option>
                      {% else %}
                      <option value="1">{{ text_enabled }}</option>
                      <option value="0" selected="selected">{{ text_disabled }}</option>
                      {% endif %}
                     </select>
                  </div>
                </div>
              </fieldset>           
            </div>         
            {% if vendor_id %}
            <div class="tab-pane" id="tab-history">
              <fieldset>
                <legend>{{ text_history }}</legend>
                <div id="history"></div>
              </fieldset>
              <br />
              <fieldset>
                <legend>{{ text_history_add }}</legend>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-comment">{{ entry_comment }}</label>
                  <div class="col-sm-10">
                    <textarea name="comment" rows="8" placeholder="{{ entry_comment }}" id="input-comment" class="form-control"></textarea>
                  </div>
                </div>
              </fieldset>
              <div class="text-right">
                <button id="button-history" data-loading-text="{{ text_loading }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i> {{ button_history_add }}</button>
              </div>
            </div>
            <div class="tab-pane" id="tab-transaction">
              <fieldset>
                <legend>{{ text_transaction }}</legend>
                <div id="transaction"></div>
              </fieldset>
              <br />
              <fieldset>
                <legend>{{ text_transaction_add }}</legend>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-transaction-description">{{ entry_description }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="description" value="" placeholder="{{ entry_description }}" id="input-transaction-description" class="form-control" />
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-amount">{{ entry_amount }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="amount" value="" placeholder="{{ entry_amount }}" id="input-amount" class="form-control" />
                  </div>
                </div>
              </fieldset>
              <div class="text-right">
                <button type="button" id="button-transaction" data-loading-text="{{ text_loading }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i> {{ button_transaction_add }}</button>
              </div>
            </div>
            <div class="tab-pane" id="tab-reward">
              <fieldset>
                <legend>{{ text_reward }}</legend>
                <div id="reward"></div>
              </fieldset>
              <br />
              <fieldset>
                <legend>{{ text_reward_add }}</legend>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-reward-description">{{ entry_description }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="description" value="" placeholder="{{ entry_description }}" id="input-reward-description" class="form-control" />
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-points"><span data-toggle="tooltip" title="{{ help_points }}">{{ entry_points }}</span></label>
                  <div class="col-sm-10">
                    <input type="text" name="points" value="" placeholder="{{ entry_points }}" id="input-points" class="form-control" />
                  </div>
                </div>
              </fieldset>
              <div class="text-right">
                <button type="button" id="button-reward" data-loading-text="{{ text_loading }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i> {{ button_reward_add }}</button>
              </div>
            </div>
            {% endif %}
            <div class="tab-pane" id="tab-ip">
              <fieldset>
                <legend>{{ text_ip }}</legend>
                <div id="ip"></div>
              </fieldset>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script type="text/javascript"><!--
  $('input[name="affiliate"]').on('change', function() {
    if ($(this).val() == '1') { 
      $('#tab-affiliate :input').not('input[name="affiliate"]').prop('disabled', false);
    } else {
      $('#tab-affiliate :input').not('input[name="affiliate"]').prop('disabled', true);
    }
  }); 

  $('input[name=\'affiliate\']:checked').trigger('change');
  //--></script> 
  <script type="text/javascript"><!--
  $('select[name=\'vendor_group_id\']').on('change', function() {
    $.ajax({
      url: 'index.php?route=vendor/vendor/customfield&user_token={{ user_token }}&vendor_group_id=' + this.value,
      dataType: 'json',
      success: function(json) {
        $('.custom-field').hide();
        $('.custom-field').removeClass('required');

        for (i = 0; i < json.length; i++) {
          vendor_field = json[i];

          $('.custom-field' + vendor_field['vendor_field_id']).show();

          if (vendor_field['required']) {
            $('.custom-field' + vendor_field['vendor_field_id']).addClass('required');
          }
        }
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  });

  $('select[name=\'vendor_group_id\']').trigger('change');
  //--></script> 
  <script type="text/javascript"><!--
  var vendor_address_row = {{ vendor_address_row }};

  function addAddress() {
    html  = '<div class="tab-pane" id="tab-vendor_address' + vendor_address_row + '">';
    html += '  <input type="hidden" name="vendor_address[' + vendor_address_row + '][vendor_address_id]" value="" />';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-firstname' + vendor_address_row + '">{{ entry_firstname }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][firstname]" value="" placeholder="{{ entry_firstname }}" id="input-firstname' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-lastname' + vendor_address_row + '">{{ entry_lastname }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][lastname]" value="" placeholder="{{ entry_lastname }}" id="input-lastname' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group">';
    html += '    <label class="col-sm-2 control-label" for="input-company' + vendor_address_row + '">{{ entry_company }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][company]" value="" placeholder="{{ entry_company }}" id="input-company' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-vendor_address-1' + vendor_address_row + '">{{ entry_vendor_address_1 }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][vendor_address_1]" value="" placeholder="{{ entry_vendor_address_1 }}" id="input-vendor_address-1' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group">';
    html += '    <label class="col-sm-2 control-label" for="input-vendor_address-2' + vendor_address_row + '">{{ entry_vendor_address_2 }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][vendor_address_2]" value="" placeholder="{{ entry_vendor_address_2 }}" id="input-vendor_address-2' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-city' + vendor_address_row + '">{{ entry_city }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][city]" value="" placeholder="{{ entry_city }}" id="input-city' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-postcode' + vendor_address_row + '">{{ entry_postcode }}</label>';
    html += '    <div class="col-sm-10"><input type="text" name="vendor_address[' + vendor_address_row + '][postcode]" value="" placeholder="{{ entry_postcode }}" id="input-postcode' + vendor_address_row + '" class="form-control" /></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-country' + vendor_address_row + '">{{ entry_country }}</label>';
    html += '    <div class="col-sm-10"><select name="vendor_address[' + vendor_address_row + '][country_id]" id="input-country' + vendor_address_row + '" onchange="country(this, \'' + vendor_address_row + '\', \'0\');" class="form-control">';
      html += '         <option value="">{{ text_select }}</option>';
      {% for country in countries %}
      html += '         <option value="{{ country.country_id }}">{{ country.name|escape('js') }}</option>';
      {% endfor %}
      html += '      </select></div>';
    html += '  </div>';

    html += '  <div class="form-group required">';
    html += '    <label class="col-sm-2 control-label" for="input-zone' + vendor_address_row + '">{{ entry_zone }}</label>';
    html += '    <div class="col-sm-10"><select name="vendor_address[' + vendor_address_row + '][zone_id]" id="input-zone' + vendor_address_row + '" class="form-control"><option value="">{{ text_none }}</option></select></div>';
    html += '  </div>';

    // Custom Fields
    {% for vendor_field in vendor_fields %}
    {% if vendor_field.location == 'vendor_address' %}
    {% if vendor_field.type == 'select' %}

    html += '  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '  		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field.name|e('js') }}</label>';
    html += '  		<div class="col-sm-10">';
    html += '  		  <select name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">';
    html += '  			<option value="">{{ text_select }}</option>';

    {% for vendor_field_value in vendor_field.vendor_field_value %}
    html += '  			<option value="{{ vendor_field_value.vendor_field_value_id }}">{{ vendor_field.name|e('js') }}</option>';
    {% endfor %}

    html += '  		  </select>';
    html += '  		</div>';
    html += '  	  </div>';
    {% endif %}

    {% if vendor_field.type == 'radio' %}
    html += '  	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}">';
    html += '  		<label class="col-sm-2 control-label">{{ vendor_field.name|e('js') }}</label>';
    html += '  		<div class="col-sm-10">';
    html += '  		  <div>';

    {% for vendor_field_value in vendor_field.vendor_field_value %}
    html += '  			<div class="radio"><label><input type="radio" name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field_value.vendor_field_value_id }}" /> {{ vendor_field_value.name|e('js') }}</label></div>';
    {% endfor %}

    html += '		  </div>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'checkbox' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label">{{ vendor_field.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <div>';

    {% for vendor_field_value in vendor_field.vendor_field_value %}
    html += '			<div class="checkbox"><label><input type="checkbox" name="vendor_address[{{ vendor_address_row }}][vendor_field][{{ vendor_field.vendor_field_id }}][]" value="{{ vendor_field_value.vendor_field_value_id }}" /> {{ vendor_field_value.name|e('js') }}</label></div>';
    {% endfor %}

    html += '		  </div>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'text' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field_value.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <input type="text" name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field.value|e('js') }}" placeholder="{{ vendor_field_value.name|e('js') }}" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" />';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'textarea' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field_value.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <textarea name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" rows="5" placeholder="{{ vendor_field_value.name|e('js') }}" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control">{{ vendor_field.value|e('js') }}</textarea>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'file' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label">{{ vendor_field.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <button type="button" id="button-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" data-loading-text="{{ text_loading }}" class="btn btn-default"><i class="fa fa-upload"></i> {{ button_upload }}</button>';
    html += '		  <input type="hidden" name="vendor_address[' + vendor_address_row + '][{{ vendor_field.vendor_field_id }}]" value="" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" />';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'date' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field_value.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <div class="input-group date"><input type="text" name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field.value|e('js') }}" placeholder="{{ vendor_field.name|e('js') }} data-date-format="YYYY-MM-DD" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'time' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field_value.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <div class="input-group time"><input type="text" name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field.value|e('js') }}" placeholder="{{ vendor_field.name|e('js') }}" data-date-format="HH:mm" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% if vendor_field.type == 'datetime' %}
    html += '	  <div class="form-group custom-field custom-field{{ vendor_field.vendor_field_id }}" data-sort="{{ vendor_field.sort_order + 1 }}">';
    html += '		<label class="col-sm-2 control-label" for="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}">{{ vendor_field_value.name|e('js') }}</label>';
    html += '		<div class="col-sm-10">';
    html += '		  <div class="input-group datetime"><input type="text" name="vendor_address[' + vendor_address_row + '][vendor_field][{{ vendor_field.vendor_field_id }}]" value="{{ vendor_field.value|e('js') }}" placeholder="{{ vendor_field.name|e('js') }}" data-date-format="YYYY-MM-DD HH:mm" id="input-vendor_address' + vendor_address_row + '-custom-field{{ vendor_field.vendor_field_id }}" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div>';
    html += '		</div>';
    html += '	  </div>';
    {% endif %}

    {% endif %}
    {% endfor %}

    html += '  <div class="form-group">';
    html += '    <label class="col-sm-2 control-label">{{ entry_default }}</label>';
    html += '    <div class="col-sm-10"><label class="radio"><input type="radio" name="vendor_address[' + vendor_address_row + '][default]" value="1" /></label></div>';
    html += '  </div>';

    html += '</div>';

    $('#tab-general .tab-content').append(html);

    $('select[name=\'vendor_group_id\']').trigger('change');

    $('select[name=\'vendor_address[' + vendor_address_row + '][country_id]\']').trigger('change');

    $('#vendor_address-add').before('<li><a href="#tab-vendor_address' + vendor_address_row + '" data-toggle="tab"><i class="fa fa-minus-circle" onclick="$(\'#vendor_address a:first\').tab(\'show\'); $(\'a[href=\\\'#tab-vendor_address' + vendor_address_row + '\\\']\').parent().remove(); $(\'#tab-vendor_address' + vendor_address_row + '\').remove();"></i> {{ tab_vendor_address }} ' + vendor_address_row + '</a></li>');

    $('#vendor_address a[href=\'#tab-vendor_address' + vendor_address_row + '\']').tab('show');

    $('.date').datetimepicker({
		language: '{{ datepicker }}',
		pickTime: false
    });

	$('.datetime').datetimepicker({
		language: '{{ datepicker }}',
		pickDate: true,
		pickTime: true
    });

    $('.time').datetimepicker({
		language: '{{ datepicker }}',
		pickDate: false
    });

    $('#tab-vendor_address' + vendor_address_row + ' .form-group[data-sort]').detach().each(function() {
		if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('#tab-vendor_address' + vendor_address_row + ' .form-group').length) {
			$('#tab-vendor_address' + vendor_address_row + ' .form-group').eq($(this).attr('data-sort')).before(this);
		}
		
		if ($(this).attr('data-sort') > $('#tab-vendor_address' + vendor_address_row + ' .form-group').length) {
			$('#tab-vendor_address' + vendor_address_row + ' .form-group:last').after(this);
		}
		
		if ($(this).attr('data-sort') < -$('#tab-vendor_address' + vendor_address_row + ' .form-group').length) {
			$('#tab-vendor_address' + vendor_address_row + ' .form-group:first').before(this);
		}
    });

    vendor_address_row++;
  }
  //--></script> 
  <script type="text/javascript"><!--
  function country(element, index, zone_id) {
    $.ajax({
      url: 'index.php?route=localisation/country/country&user_token={{ user_token }}&country_id=' + element.value,
      dataType: 'json',
      beforeSend: function() {
        $('select[name=\'vendor_address[' + index + '][country_id]\']').prop('disabled', true);
      },
      complete: function() {
        $('select[name=\'vendor_address[' + index + '][country_id]\']').prop('disabled', false);
      },
      success: function(json) {
        if (json['postcode_required'] == '1') {
          $('input[name=\'vendor_address[' + index + '][postcode]\']').parent().parent().addClass('required');
        } else {
          $('input[name=\'vendor_address[' + index + '][postcode]\']').parent().parent().removeClass('required');
        }

        html = '<option value="">{{ text_select }}</option>';

        if (json['zone'] && json['zone'] != '') {
          for (i = 0; i < json['zone'].length; i++) {
            html += '<option value="' + json['zone'][i]['zone_id'] + '"';

            if (json['zone'][i]['zone_id'] == zone_id) {
              html += ' selected="selected"';
            }

            html += '>' + json['zone'][i]['name'] + '</option>';
          }
        } else {
          html += '<option value="0">{{ text_none }}</option>';
        }

        $('select[name=\'vendor_address[' + index + '][zone_id]\']').html(html);
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  }

  $('select[name$=\'[country_id]\']').trigger('change');
  //--></script> 
  <script type="text/javascript"><!--
  $('#history').delegate('.pagination a', 'click', function(e) {
    e.preventDefault();

    $('#history').load(this.href);
  });

  $('#history').load('index.php?route=vendor/vendor/history&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

  $('#button-history').on('click', function(e) {
    e.preventDefault();

    $.ajax({
      url: 'index.php?route=vendor/vendor/addhistory&user_token={{ user_token }}&vendor_id={{ vendor_id }}',
      type: 'post',
      dataType: 'json',
      data: 'comment=' + encodeURIComponent($('#tab-history textarea[name=\'comment\']').val()),
      beforeSend: function() {
        $('#button-history').button('loading');
      },
      complete: function() {
        $('#button-history').button('reset');
      },
      success: function(json) {
        $('.alert-dismissible').remove();

        if (json['error']) {
           $('#tab-history').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
        }

        if (json['success']) {
          $('#tab-history').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');

          $('#history').load('index.php?route=vendor/vendor/history&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

          $('#tab-history textarea[name=\'comment\']').val('');
        }
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  });
  //--></script> 
  <script type="text/javascript"><!--
  $('#transaction').delegate('.pagination a', 'click', function(e) {
    e.preventDefault();

    $('#transaction').load(this.href);
  });

  $('#transaction').load('index.php?route=vendor/vendor/transaction&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

  $('#button-transaction').on('click', function(e) {
    e.preventDefault();

    $.ajax({
      url: 'index.php?route=vendor/vendor/addtransaction&user_token={{ user_token }}&vendor_id={{ vendor_id }}',
      type: 'post',
      dataType: 'json',
      data: 'description=' + encodeURIComponent($('#tab-transaction input[name=\'description\']').val()) + '&amount=' + encodeURIComponent($('#tab-transaction input[name=\'amount\']').val()),
      beforeSend: function() {
        $('#button-transaction').button('loading');
      },
      complete: function() {
        $('#button-transaction').button('reset');
      },
      success: function(json) {
        $('.alert-dismissible').remove();

        if (json['error']) {
           $('#tab-transaction').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
        }

        if (json['success']) {
          $('#tab-transaction').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');

          $('#transaction').load('index.php?route=vendor/vendor/transaction&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

          $('#tab-transaction input[name=\'amount\']').val('');
          $('#tab-transaction input[name=\'description\']').val('');
        }
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  });
  //--></script> 
  <script type="text/javascript"><!--
  $('#reward').delegate('.pagination a', 'click', function(e) {
    e.preventDefault();

    $('#reward').load(this.href);
  });

  $('#reward').load('index.php?route=vendor/vendor/reward&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

  $('#button-reward').on('click', function(e) {
    e.preventDefault();

    $.ajax({
      url: 'index.php?route=vendor/vendor/addreward&user_token={{ user_token }}&vendor_id={{ vendor_id }}',
      type: 'post',
      dataType: 'json',
      data: 'description=' + encodeURIComponent($('#tab-reward input[name=\'description\']').val()) + '&points=' + encodeURIComponent($('#tab-reward input[name=\'points\']').val()),
      beforeSend: function() {
        $('#button-reward').button('loading');
      },
      complete: function() {
        $('#button-reward').button('reset');
      },
      success: function(json) {
        $('.alert-dismissible').remove();

        if (json['error']) {
           $('#tab-reward').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
        }

        if (json['success']) {
          $('#tab-reward').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');

          $('#reward').load('index.php?route=vendor/vendor/reward&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

          $('#tab-reward input[name=\'points\']').val('');
          $('#tab-reward input[name=\'description\']').val('');
        }
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  });

  $('#ip').delegate('.pagination a', 'click', function(e) {
    e.preventDefault();

    $('#ip').load(this.href);
  });

  $('#ip').load('index.php?route=vendor/vendor/ip&user_token={{ user_token }}&vendor_id={{ vendor_id }}');

  $('#content').delegate('button[id^=\'button-custom-field\'], button[id^=\'button-vendor_address\']', 'click', function() {
    var node = this;

    $('#form-upload').remove();

    $('body').prepend('<form enctype="multipart/form-data" id="form-upload" style="display: none;"><input type="file" name="file" /></form>');

    $('#form-upload input[name=\'file\']').trigger('click');

    if (typeof timer != 'undefined') {
        clearInterval(timer);
    }

    timer = setInterval(function() {
      if ($('#form-upload input[name=\'file\']').val() != '') {
        clearInterval(timer);

        $.ajax({
          url: 'index.php?route=tool/upload/upload&user_token={{ user_token }}',
          type: 'post',
          dataType: 'json',
          data: new FormData($('#form-upload')[0]),
          cache: false,
          contentType: false,
          processData: false,
          beforeSend: function() {
            $(node).button('loading');
          },
          complete: function() {
            $(node).button('reset');
          },
          success: function(json) {
            $(node).parent().find('.text-danger').remove();

            if (json['error']) {
              $(node).parent().find('input[type=\'hidden\']').after('<div class="text-danger">' + json['error'] + '</div>');
            }

            if (json['success']) {
              alert(json['success']);
            }

            if (json['code']) {
              $(node).parent().find('input[type=\'hidden\']').val(json['code']);
            }
          },
          error: function(xhr, ajaxOptions, thrownError) {
            alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
          }
        });
      }
    }, 500);
  });

  $('.date').datetimepicker({
    language: '{{ datepicker }}',
    pickTime: false
  });

  $('.datetime').datetimepicker({
    language: '{{ datepicker }}',
    pickDate: true,
    pickTime: true
  });

  $('.time').datetimepicker({
    language: '{{ datepicker }}',
    pickDate: false
  });

  // Sort the custom fields
  {% set vendor_address_row = 1 %}
  {% for vendor_address in vendor_addresses %}
  $('#tab-vendor_address{{ vendor_address_row }} .form-group[data-sort]').detach().each(function() {
    if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('#tab-vendor_address{{ vendor_address_row }} .form-group').length) {
      $('#tab-vendor_address{{ vendor_address_row }} .form-group').eq($(this).attr('data-sort')).before(this);
    }

    if ($(this).attr('data-sort') > $('#tab-vendor_address{{ vendor_address_row }} .form-group').length) {
      $('#tab-vendor_address{{ vendor_address_row }} .form-group:last').after(this);
    }

    if ($(this).attr('data-sort') < -$('#tab-vendor_address{{ vendor_address_row }} .form-group').length) {
      $('#tab-vendor_address{{ vendor_address_row }} .form-group:first').before(this);
    }
  });
  {% set vendor_address_row = vendor_address_row + 1 %}
  {% endfor %}

  $('#tab-vendor .form-group[data-sort]').detach().each(function() {
    if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('#tab-vendor .form-group').length) {
      $('#tab-vendor .form-group').eq($(this).attr('data-sort')).before(this);
    }

    if ($(this).attr('data-sort') > $('#tab-vendor .form-group').length) {
      $('#tab-vendor .form-group:last').after(this);
    }

    if ($(this).attr('data-sort') < -$('#tab-vendor .form-group').length) {
      $('#tab-vendor .form-group:first').before(this);
    }
  });

  $('#tab-affiliate .form-group[data-sort]').detach().each(function() {
    if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('#tab-affiliate .form-group').length) {
      $('#tab-affiliate .form-group').eq($(this).attr('data-sort')).before(this);
    }

    if ($(this).attr('data-sort') > $('#tab-affiliate .form-group').length) {
      $('#tab-affiliate .form-group:last').after(this);
    }

    if ($(this).attr('data-sort') < -$('#tab-affiliate .form-group').length) {
      $('#tab-affiliate .form-group:first').before(this);
    }
  });
  //--></script> 
  <script type="text/javascript"><!--
  $('input[name=\'payment\']').on('change', function() {
    $('.payment').hide();

    $('#payment-' + this.value).show();
  });

  $('input[name=\'payment\']:checked').trigger('change');
  //--></script> 
</div>
{{ footer }} 
